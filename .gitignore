node_modules/
config.json
state/
# Token factory: per-token distributor keys and state (secret)
/factory/
*.keypair.json
*-keypair.json

# Rust build output (contains absolute local paths) and local validator ledgers
lp-locker/target/
test-ledger/

# Scratch logo candidates
logo-options/
